var players, block, ball
var Play
var gameState
var lebron
var curry
var player
var irving
var durant
var hortencia
var lessie





function draw(){
    background(0)
    if(gameState===0){
    Play()
 //machine()
 for(var i; i < player.length;i++){
    if(player.onPress("up")){
    player
    }
 }
    }
}